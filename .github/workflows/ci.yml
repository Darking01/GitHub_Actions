name: CI Pipeline Básico

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    name: 🧪 Ejecutar Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout del código
        uses: actions/checkout@v3
      
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: 📦 Instalar dependencias
        run: npm install

      # 🔍 Linting del código (Ejercicio 2)
      - name: 🔍 Ejecutar ESLint
        run: npm run lint

      - name: 🧪 Ejecutar tests
        run: npm test
      
      - name: 💻 Información del Sistema Operativo
        run: |
          echo "===================================="
          echo "INFORMACIÓN DEL RUNNER"
          echo "===================================="
          uname -a
          node --version
          npm --version
          free -h
          df -h

  build:
    name: 🏗️ Build de la Aplicación
    needs: test
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout del código
        uses: actions/checkout@v3
      
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: 📦 Instalar dependencias
        run: npm install
      
      - name: 🏗️ Ejecutar build
        run: node index.js

      # 🚀 Step condicional solo en main (Ejercicio 3)
      - name: 🚀 Deploy simulado
        if: github.ref == 'refs/heads/main'
        run: echo "Realizando deployment simulado..."

      - name: 📤 Subir artifact del build
        uses: actions/upload-artifact@v3
        with:
          name: aplicacion-build
          path: |
            index.js
            test.js
            package.json
          retention-days: 5

  workflow-info:
    name: 📊 Información del Workflow
    runs-on: ubuntu-latest
    
    steps:
      - name: 📊 Mostrar información del workflow
        run: |
          echo "===================================="
          echo "INFORMACIÓN DEL WORKFLOW"
          echo "===================================="
          echo "Nombre del repositorio: ${{ github.repository }}"
          echo "Rama: ${{ github.ref }}"
          echo "SHA del commit: ${{ github.sha }}"
          echo "Actor que disparó el workflow: ${{ github.actor }}"
          echo "Evento que disparó el workflow: ${{ github.event_name }}"
          echo "Número de ejecución: ${{ github.run_number }}"
          echo "Intento de ejecución: ${{ github.run_attempt }}"
          echo "===================================="

